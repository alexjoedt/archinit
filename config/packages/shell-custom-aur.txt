# config/packages/shell-custom-aur.txt — AUR packages for the custom Quickshell shell
# Installed by the 'shell' module only when DESKTOP_SHELL=custom.
# Kept separate from config/packages/aur.txt and the Noctalia list to avoid
# pulling a competing shell stack onto the system.
# One package per line; # and blank lines ignored.

# Quickshell — the custom bar / notifications / OSD layer
quickshell-git

# Audio visualizer used by custom shell widgets
cava
